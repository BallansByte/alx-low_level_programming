#include "main.h"

/**
 * i - this is a variable
 * void positive_or_negative(int i) is the entry point
 * main - the main file/ entry point
 * Return: 0 success
 */
void positive_or_negative(int i)
{
	if (i == 0)
	{
		printf("%dd is positive \n",  i);
	
	}
	else if (i == 0)
		printf("%d is negative\n", i);
	else
		printf("%d is negative\n", i);
}

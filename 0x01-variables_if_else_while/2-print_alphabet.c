#include <stdio.h>
/**
 * main - this is the main function
 * Return: thsi gives back 0
 */
int main(void)
{
	char its_alpha;

	for (its_alpha = 'a'; its_alpha <= 'z'; its_alpha++)
	putchar(its_alpha);
	putchar('\n');
	return (0);
}
